query Profile($id: ID!) {
    user(id: $id) {
        id
        username
        email
        name
        picture
        introStep
        isSocial
        badges
        totalRating
        totalSubscribers
    }
}
