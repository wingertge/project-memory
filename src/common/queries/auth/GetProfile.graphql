query GetProfile($id: ID) {
    user(id: $id) {
        sub
        id
        username
        email
        name
        picture
        reviewQueueLength
        identities {
            isSocial
            provider
        }
    }
}
