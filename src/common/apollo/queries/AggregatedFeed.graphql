query AggregatedFeed($userId: ID!, $filter: PostFilterInput) {
    user(id: $userId) {
        id
        subscriptionFeed(filter: $filter) {
            ...shallowPostFields, isLikedBy(userID: $userId)
            originalPost {...shallowPostFields, isLikedBy(userID: $userId)}
        }
    }
}
