query Users($limit: Int, $offset: Int, $filter: UserFilterInput, $sort: UserSortInput) {
    users(limit: $limit, offset: $offset, filter: $filter, sort: $sort) {
        id
        picture
        username
        totalRating
        totalSubscribers
    }
}
